// Entry point / composition root. Modules land here phase by phase
// as the legacy/cc-bot.original.js monolith gets ported (see AGENTS.md).
console.log('CC Good Boy: build pipeline online, bot not yet migrated.');
